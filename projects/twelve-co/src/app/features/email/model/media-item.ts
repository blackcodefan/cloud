export interface MediaItem {
    id: string,
    name: string,
    mediaUrl: string,
    mediaSize: string,
    mediaType: string,
    mediaHeight: number,
    mediaWidth: number,
    date: Date,
}

export interface LabelItem {
    id: string,
    name: string,
}

