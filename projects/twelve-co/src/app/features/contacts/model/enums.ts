export enum FieldTypes {
  department = 'department',
  jobTitle = 'job title',
  note = 'note',
  birthday = 'birthday',
}


export enum ContactDefaultGroupEnum {
  MY_CONTACTS = -1, BLOCKED = -2
}

export enum ContactVisibility {
  VISIBLE = 1, INVISIBLE = 2
}

export enum ContactSortTypeEnum {
  FIRST_NAME = 'FIRST_NAME',
  LAST_NAME = 'LAST_NAME'
}

export enum ViewMode {
  EDIT_MODE = 'EDIT_MODE', VIEW_MODE = 'VIEW_MODE', CREATE_MODE = 'CREATE_MODE'
}

export enum CsvTypeEnum {
  GOOGLE = 'CsvTypeGoogle', OUTLOOK = 'CsvTypeOutlook', GENERIC = 'CsvTypeGeneric'
}

export enum GROUP_TYPE {
  MY_CONTACTS = -1, BLOCKED = -2
}
